spring:
  application:
    name: AUTH
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/rentoome
    username: postgres
    password: megatim
    driverClassName: org.postgresql.Driver
  sql:
    init:
      platform: postgres
      
  springdoc:
    api-docs:
        path: /api-docs


# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8777/eureka/

server:
  port: 8780

jwt:
  private.key: classpath:app.key
  public.key: classpath:app.pub

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.springframework.security: INFO
#    org.springframework.boot.autoconfigure: DEBUG

auth:
  app:
    jwtSecret: d6633c8cc2599dd2ca54d0273bf9486186a6f51b745f7e5c84a720c0a2aab470cdbbc306bad14d1370100acb5c2b4ecb0dd0258de96e639c9444f2d20afbd652
    jwtExpirationMs: 86400000